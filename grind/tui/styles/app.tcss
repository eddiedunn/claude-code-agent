/* Agent Orchestration TUI Styles */

/* Screen layout */
Screen {
    layout: vertical;
}

Header {
    height: auto;
    background: $boost;
    color: $text;
}

Footer {
    height: auto;
    background: $boost;
    color: $text;
}

/* Status Bar */
#status-bar {
    height: 1;
    background: $surface;
    color: $text;
    padding: 0 1;
}

/* TabbedContent styling */
TabbedContent {
    height: 1fr;
    width: 1fr;
}

#main-tabs {
    height: 1fr;
    width: 1fr;
}

/* Tab bar appearance */
TabbedContent > ContentSwitcher {
    background: $surface;
}

Tabs {
    height: auto;
    background: $surface;
}

/* Tab button styling */
Tab {
    background: $surface;
    color: $text;
    padding: 0 2;
    text-style: none;
}

Tab:hover {
    background: $boost;
}

/* Active tab highlighting */
Tab.-active {
    background: $accent;
    color: $text;
    text-style: bold;
}

/* Tab content padding */
TabPane {
    height: 1fr;
    width: 1fr;
    padding: 0;
}

/* ListView styling for running agents */
ListView {
    height: 1fr;
    background: $surface;
    scrollbar-background: $surface;
    scrollbar-color: $accent;
}

ListView > ListItem {
    padding: 0 1;
    background: $surface;
}

/* Hover state for list items */
ListView > ListItem:hover {
    background: $boost;
}

/* Selection highlighting */
ListView > ListItem.-highlight {
    background: $accent;
    color: $text;
}

/* Scrollbar styling for ListView */
ListView:focus {
    border: tall $accent;
}

/* DataTable styling for completed agents */
DataTable {
    height: 1fr;
    background: $surface;
}

/* Header row styling */
DataTable > .datatable--header {
    background: $boost;
    color: $text;
    text-style: bold;
}

/* Cursor/selected row */
DataTable > .datatable--cursor {
    background: $accent;
}

/* Hover state */
DataTable:hover {
    border: tall $accent;
}

/* Zebra striping for data rows */
DataTable > .datatable--odd {
    background: $surface;
}

DataTable > .datatable--even {
    background: $boost;
}

/* Scrollbar for DataTable */
DataTable {
    scrollbar-background: $surface;
    scrollbar-color: $accent;
    scrollbar-color-hover: $accent;
    scrollbar-color-active: $accent;
}

/* Log viewer styling - terminal-like appearance with monospace */
StreamingLogViewer {
    background: #1a1a2e;
    color: #e0e0e0;
    scrollbar-background: #1a1a2e;
    scrollbar-color: #4a4a6a;
    scrollbar-color-hover: #6a6a8a;
    scrollbar-color-active: #8a8aaa;
}

/* Syntax highlighting colors for log viewer */
StreamingLogViewer .log-error {
    color: $error;
    text-style: bold;
}

StreamingLogViewer .log-warning {
    color: $warning;
}

StreamingLogViewer .log-success {
    color: $success;
}

StreamingLogViewer .log-info {
    color: $accent;
}

StreamingLogViewer .log-timestamp {
    color: $text-muted;
}

/* Semantic colors for status */
.status-complete {
    color: $success;
}

.status-failed {
    color: $error;
}

.status-stuck {
    color: $error;
    text-style: bold;
}

.status-running {
    color: $warning;
    text-style: bold;
}

.status-pending {
    color: $text-muted;
}

.status-paused {
    color: magenta;
    text-style: bold;
}

/* DAG placeholder styling */
#dag-placeholder {
    content-align: center middle;
    color: $text-muted;
}

/* Container styling */
.agent-overview {
    height: 1fr;
    padding: 1;
}

/* Shell styling - Terminal-like appearance */
AgentShell {
    height: 1fr;
    width: 1fr;
    background: $surface;
    layout: vertical;
}

AgentShell #shell-output {
    height: 1fr;
    width: 1fr;
    background: #1a1a2e;
    padding: 0 1;
    scrollbar-background: #1a1a2e;
    scrollbar-color: #4a4a6a;
    scrollbar-color-hover: #6a6a8a;
    scrollbar-color-active: #8a8aaa;
}

AgentShell #output-text {
    width: 1fr;
    background: #1a1a2e;
    color: #e0e0e0;
}

/* Shell prompt - Green terminal style */
AgentShell #prompt-container {
    height: auto;
    width: 1fr;
    background: #1a1a2e;
    padding: 0 1;
}

AgentShell #shell-prompt {
    width: auto;
    color: #00ff00;
    text-style: bold;
    background: transparent;
}

/* Shell input field */
AgentShell #shell-input {
    width: 1fr;
    background: #1a1a2e;
    color: #e0e0e0;
    border: none;
}

AgentShell #shell-input:focus {
    border: none;
}

/* Shell completions popup */
AgentShell #completions-popup {
    display: none;
    background: #2a2a4e;
    border: solid #4a4a6a;
    padding: 0 1;
    max-height: 10;
}

AgentShell #completions-popup.visible {
    display: block;
}
